FROM postgres:16-alpine3.17

ARG DB_USER
ARG DB_PASSWORD
ARG DB_NAME
ARG DB_PORT

RUN test -n "$DB_USER, $DB_PASSWORD, $DB_NAME, $DB_PORT"

ENV POSTGRES_USER=${DB_USER}
ENV POSTGRES_PASSWORD=${DB_PASSWORD}
ENV POSTGRES_DB=${DB_NAME}
EXPOSE ${DB_PORT}
CMD ["postgres"]